#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='flsd',
      version='1.0',
      description='FLSD-Sandbox',
      author='Sam Sinai',
      packages=find_packages(),
     )